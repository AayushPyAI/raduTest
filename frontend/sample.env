# =============================================================================
# Patent Search Frontend - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your actual values
# DO NOT commit .env files with real secrets to version control

# -----------------------------------------------------------------------------
# API Configuration
# -----------------------------------------------------------------------------
# Backend API URL (update port if backend runs on different port)
REACT_APP_API_URL=http://localhost:8000

# -----------------------------------------------------------------------------
# Firebase Authentication Configuration
# -----------------------------------------------------------------------------
# Get all values from: Firebase Console > Project Settings > General > Your apps
# Click on the web app and copy the config values

# Firebase API key for web applications
REACT_APP_FIREBASE_API_KEY=your-firebase-api-key

# Firebase auth domain (usually project-id.firebaseapp.com)
REACT_APP_FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com

# Firebase project ID
REACT_APP_FIREBASE_PROJECT_ID=your-firebase-project-id

# Firebase storage bucket (usually project-id.appspot.com)
REACT_APP_FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com

# Firebase messaging sender ID
REACT_APP_FIREBASE_MESSAGING_SENDER_ID=123456789012

# Firebase app ID
REACT_APP_FIREBASE_APP_ID=1:123456789012:web:abcdef123456789012345

# -----------------------------------------------------------------------------
# Setup Instructions
# -----------------------------------------------------------------------------
# 1. Create a Firebase project at https://console.firebase.google.com
# 2. Enable Authentication with Email/Password and Google providers
# 3. Add your domain (localhost:3005) to authorized domains in Authentication settings
# 4. Get the config values from Project Settings > General > Your apps
# 5. Replace the placeholder values above with your actual Firebase config
# 6. Make sure your backend is running on the correct port (default: 8000) 